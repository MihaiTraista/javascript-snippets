// Write a function that adds a reverse method to the String prototype. The method should reverse the string and return the reversed version.

// String.prototype.reverse

const b1 = false;
const b2 = new Boolean(true);

